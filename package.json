{
  "name": "react_native_mqtt",
  "version": "1.1.5",
  "description": "Drop in mqtt client for react native.",
  "repository": {
    "type": "git",
    "url": "https://github.com/Introvertuous/react_native_mqtt"
  },
  "dependencies": {
    "react-native-storage": "^0.1.2"
  },
  "main": "dist/src/index.js",
  "devDependencies": {
    "@types/node": "^7.0.21",
    "@types/jest": "^19.2.3",
    "jest-cli": "^20.0.3",
    "typedoc": "^0.7.1",
    "tslint": "5.2.0",
    "tslint-config-standard": "^5.0.2",
    "tslint-react": "^3.0.0",
    "standard-version": "^4.0.0",
    "typescript": "^2.3.2"
  },
  "scripts": {
    "docs": "typedoc --out docs src",
    "build": "tsc",
    "test": "jest dist",
    "release": "yarn build && yarn docs && standard-version"
  },
  "jest": {
    "verbose": true
  }
}
